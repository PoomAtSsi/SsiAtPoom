<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC
"-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="noticeMapper">

	<resultMap id="noticeResult" type="notice">
		<id property="NOTICE_NO" column="NOTICE_NO" />
		<result property="NOTICE_TITLE" column="NOTICE_TITLE" />
		<result property="NOTICE_WRITER" column="NOTICE_WRITER" />
		<result property="NOTICE_DATE" column="NOTICE_DATE" />
		<result property="NOTICE_CONTENT" column="NOTICE_CONTENT" />
		<result property="NOTICE_VIEWS" column="NOTICE_VIEWS" />
	</resultMap>
	
	<select id="noticeList" resultMap="noticeResult">
		SELECT * FROM NOTICE2
	</select>
	<select id="noticeInsert2" parameterType="hashmap" resultMap="noticeResult">
		SELECT * FROM NOTICE2
	</select>
	
	
	<insert id="noticeInsert" parameterType="notice">
		 <![CDATA[
		insert into NOTICE2 values (
			test_seq.nextval, 
			NOTICE_TITLE = #{NOTICE_TITLE}, 
			NOTICE_WRITER = #{NOTICE_WRITER}, 
			NOTICE_DATE = SYSDATE, 
			NOTICE_CONTENT = #{NOTICE_CONTENT}, 
			NOTICE_VIEWS = 0);
		]]>
	</insert>
	
	
	<select id="noticeDetail" resultMap="noticeResult" parameterType="int" >
		SELECT * FROM NOTICE2 where NOTICE_NO = #{notice_no}
	</select>
	
	<!-- <select id="noticeUpdateViewsCnt" parameterType="hashmap" resultType="hashmap">
		 <![CDATA[
        UPDATE NOTICE
        SET	NOTICE_VIEWS = NVL(NOTICE_VIEWS, 0) + 1
        WHERE NOTICE_NO = #{NOTICE_NO}   
    	]]>

	</select> -->
	
	

</mapper>
